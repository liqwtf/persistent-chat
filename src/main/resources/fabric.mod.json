{
  "schemaVersion": 1,
  "name": "Persistent Chat",
  "id": "persistent-chat",
  "description": "${description}",
  "version": "${version}",
  "authors": [ "liqw" ],
  "contact": {
    "sources": "${sources}",
    "issues": "${issues}"
  },
  "license": "MIT",
  "icon": "assets/persistent-chat/icon.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "dev.liqw.persistentchat.PersistentChat"
    ]
  },
  "mixins": [
    "persistent-chat.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader}",
    "minecraft": "${minecraft}"
  }
}